version: '3.8'

services:
  sentiment-app:
    image: anasnasu26/sentiment-genai-app:latest
    build: .
    ports:
      - "5000:5000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    restart: unless-stopped
